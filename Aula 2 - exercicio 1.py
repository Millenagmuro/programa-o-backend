from turtle import*
for steps in range(100):
    for c in ('blue', 'red', 'green'):
        color(c)
        left(10)
        forward(steps)
        right(50)
        
clearscreen()
home()

                     
